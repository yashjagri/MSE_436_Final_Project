DATABASE_URL=postgresql://localhost/soccer_idss

# Optional: set to 1 to re-scrape even if cached JSON exists
FORCE_RESCRAPE=0

# Delay between requests (seconds). Increase if getting rate-limited.
SCRAPE_DELAY_SECONDS=2.0
